spring:
  application:
    name: hexagonal-architecture
  datasource:
    driver-class-name: org.h2.Driver
    url: "jdbc:h2:mem:testDb"
    username: sa
    password: sa
    hikari:
      idle-timeout: 300000
      minimum-idle: 1
      maximum-pool-size: 5